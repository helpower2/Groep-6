using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HumanStats : MonoBehaviour
{
    private float speed;//100 is normal, 150 very fast, 80 slow
    private float health;//20 is defualt // 40 is buff // 60 is boss;
    private float cost; //the cost per hour of work, 15 is low, 25 is normal, 35 is high
    private float damage; //

    private List<GameObject> Equipment;// maak dit naar een enum als je het later gaat gebruiken 
    
}
